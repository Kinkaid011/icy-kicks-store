import { SecondFilterPipe } from './second-filter.pipe';

describe('SecondFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
